# 狼人杀AI智能体项目依赖 - 仅支持DashScope模型
# AgentScope框架默认安装，此处仅列出自定义依赖

# 核心依赖
pydantic>=2.0.0
typing-extensions>=4.0.0
PyYAML>=6.0.0
python-dotenv>=1.0.0

# DashScope模型支持依赖
dashscope>=1.0.0  # 阿里云通义千问（必需）

# 开发和测试依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0
